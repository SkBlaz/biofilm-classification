name: microics

# Optional developer convenience. The production image does not require Compose.
services:
  microics:
    build:
      context: .
      dockerfile: Dockerfile
    image: microics:dev
    ports:
      - "8765:8765"
    volumes:
      - microics-data:/data

volumes:
  microics-data:
